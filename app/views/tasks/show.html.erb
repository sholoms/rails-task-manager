<h1> <%= @task.title %> </h1>
<p> <%= @task.details %> </p>
<p>This task is <%= @task.completed ? '' : 'not' %> complete</p>
<%= link_to 'Edit Task', edit_task_path(@task) %>
<%= link_to 'Delete Task', task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %>
<br>
<%= link_to 'Home', tasks_path %>
